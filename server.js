/**
 * Cloudflare Pages: `npm run build` then `npm run start` (wrangler pages dev dist).
 * The Cloudflare adapter does not emit dist/server/entry.mjs; do not use `node server.js` for this target.
 */
