# Render.com Blueprint for toruabii.ee
# https://docs.render.com/blueprint-spec
# Set PUBLIC_GOOGLE_REVIEW_URL in Render Dashboard → Environment (see .env.example)

services:
  - type: web
    runtime: node
    name: toruabii
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    healthCheckPath: /
